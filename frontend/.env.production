# Production API base (baked into the build at build time).
# Render single-server backend. After changing this, REBUILD/redeploy the
# Vercel frontend — Vite embeds the value into the bundle.
VITE_API_BASE_URL=https://ai-eductional-assistent.onrender.com/api/v1
