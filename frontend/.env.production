# Production API base (baked into the build at build time).
# Render single-server URL — replace <ai-study-companion> if your Render
# service name differs, then REBUILD/redeploy the Vercel frontend.
VITE_API_BASE_URL=https://ai-study-companion.onrender.com/api/v1
